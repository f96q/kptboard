- flash.each do |type, message|
  .alert.alert-dismissable{class: 'alert-' + {'error' => 'danger', 'info' => 'info', 'notice' => 'success', 'warning' => 'warning'}[type]}
    %button{type: 'button', class: 'close', :'data-dismiss' => 'alert', :'aria-hidden' => 'true'} &times;
    = message
