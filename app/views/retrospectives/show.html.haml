= content_for :active, 'retrospectives'
= content_for :navigation_title, @retrospective.title

.retrospective
  = react_component('RetrospectiveBoards', url: "#{request.host}:#{request.port}/websocket", id: @retrospective.id)
